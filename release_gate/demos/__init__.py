"""Demonstrations built on release-gate's public API.

Nothing here is imported by the engine. These modules are consumers, like any
other client would be: if a demo needs something the engine cannot do, that is a
finding about the engine rather than a reason to add a hook for the demo.
"""
